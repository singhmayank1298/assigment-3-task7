import React from "react";

const AuthContext= React.createContext(); // it will make a object it take default state ,we can pass any data type
                                         // default value is useable when you does not creat Provider
export default AuthContext